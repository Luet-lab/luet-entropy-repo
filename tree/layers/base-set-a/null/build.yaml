unpack: true
steps:
- >-
  source /etc/profile && (ACCEPT_LICENSE=* equo i ${ORIGINAL_PACKAGE} || emerge  =${ORIGINAL_PACKAGE})
  && equo cleanup
requires:
- category: "layer"
  name: "sabayon-base"
  version: "9999"
env:
- ORIGINAL_ATOM=layer/sabayon-base-set-a
- ORIGINAL_PACKAGE=layer/sabayon-base-set-a-null
includes:
