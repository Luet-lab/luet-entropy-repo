requires:
- category: "distro"
  name: "alpine"
  version: "3.11.3"
env:
- ORIGINAL_ATOM=x11-base/xorg-drivers
- ORIGINAL_PACKAGE=x11-base/xorg-drivers-null
steps:
- >-
  source /etc/profile && (ACCEPT_LICENSE=* equo i ${ORIGINAL_PACKAGE} || emerge  =${ORIGINAL_PACKAGE})
  && equo cleanup
includes:
