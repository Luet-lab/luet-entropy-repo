
steps:

- source /etc/profile && echo Prepare app-crypt/pinentry-gnome-1.1.0-r2
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /usr$
- /usr/bin$
- /usr/bin/pinentry-gnome3$
- /usr/lib$
