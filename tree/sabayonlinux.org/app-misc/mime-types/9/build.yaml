
unpack: true
steps:
- source /etc/profile && ACCEPT_LICENSE=* equo i app-misc/mime-types-9 && equo cleanup
requires:
- category: "layer"
  name: "sabayon-base"
  version: "9999"
includes:
- /etc$
- /etc/mime.types$
