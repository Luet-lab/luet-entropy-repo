
steps:

- source /etc/profile && echo Prepare app-misc/c_rehash-1.7-r1
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /usr$
- /usr/bin$
- /usr/bin/c_rehash$
