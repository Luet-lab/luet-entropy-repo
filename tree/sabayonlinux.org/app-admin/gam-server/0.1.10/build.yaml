
steps:

- source /etc/profile && echo Prepare app-admin/gam-server-0.1.10-r2
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /usr$
- /usr/lib$
- /usr/libexec$
- /usr/libexec/gam_server$
