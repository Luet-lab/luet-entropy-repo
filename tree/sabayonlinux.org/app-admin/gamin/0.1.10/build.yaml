
steps:
- source /etc/profile && equo up
- source /etc/profile && ACCEPT_LICENSE=* equo i app-admin/gamin-0.1.10-r1
unpack: true
requires:
- category: "layer"
  name: "sabayon-base"
  version: "9999"
