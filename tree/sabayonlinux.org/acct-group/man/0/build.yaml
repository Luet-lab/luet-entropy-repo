
steps:
- source /etc/profile && equo up
- source /etc/profile && ACCEPT_LICENSE=* equo i acct-group/man-0
unpack: true
requires:
  category: "layer"
  name: "sabayon-base"
  version: "9999"
includes:
