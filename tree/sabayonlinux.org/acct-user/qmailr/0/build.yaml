
steps:
- source /etc/profile && equo up
- source /etc/profile && ACCEPT_LICENSE=* equo i acct-user/qmailr-0
unpack: true
requires:
- category: "layer"
  name: "sabayon-base"
  version: "9999"
includes:
