
steps:

- source /etc/profile && echo Prepare acct-user/ceph-0
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /var$
- /var/lib$
- /var/lib/ceph$
- /var/lib/ceph/.keep_acct-user_ceph-0$
