
steps:
- source /etc/profile && equo up
- source /etc/profile && ACCEPT_LICENSE=* equo i acct-user/mail-0
unpack: true
requires:
  category: "layer"
  name: "sabayon-base"
  version: "9999"
includes:
- /var$
- /var/spool$
- /var/spool/mail$
- /var/spool/mail/.keep_acct-user_mail-0$
