
steps:

- source /etc/profile && echo Prepare acct-user/mail-0
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /var$
- /var/spool$
- /var/spool/mail$
- /var/spool/mail/.keep_acct-user_mail-0$
