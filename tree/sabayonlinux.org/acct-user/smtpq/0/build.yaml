
steps:

- source /etc/profile && echo Prepare acct-user/smtpq-0
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /var$
- /var/empty$
- /var/empty/.keep_acct-user_smtpq-0$
