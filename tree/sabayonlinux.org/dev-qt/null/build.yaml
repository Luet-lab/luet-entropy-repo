requires:
- category: "distro"
  name: "alpine"
  version: "3.11.3"
env:
- ORIGINAL_ATOM=dev-qt/qtsql
- ORIGINAL_PACKAGE=dev-qt/qtsql-null
steps:
- >-
  source /etc/profile && (ACCEPT_LICENSE=* equo i ${ORIGINAL_PACKAGE} || emerge  =${ORIGINAL_PACKAGE})
  && equo cleanup
includes:
