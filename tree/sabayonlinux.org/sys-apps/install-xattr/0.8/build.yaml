
steps:

- source /etc/profile && echo Prepare sys-apps/install-xattr-0.8
unpack: true
requires:
- category: "layer"
  name: "sabayon-base-set-a"
  version: "9999"
includes:
- /usr$
- /usr/bin$
- /usr/bin/install-xattr$
- /usr/lib$
