steps:
- >-
  source /etc/profile && (ACCEPT_LICENSE=* equo i ${ORIGINAL_PACKAGE} || emerge  =${ORIGINAL_PACKAGE})
  && equo cleanup
requires:
- category: "layer"
  name: "sabayon-base"
  version: "9999"
env:
- ORIGINAL_ATOM=sys-apps/sabayon-lib-migration
- ORIGINAL_PACKAGE=sys-apps/sabayon-lib-migration-null
includes:
